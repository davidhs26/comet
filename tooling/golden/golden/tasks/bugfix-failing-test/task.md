# Bugfix: test que falla

En este workspace hay un módulo Node (`src/cart.mjs`) con un bug y un test (`cart.test.mjs`) que falla.

1. Corré `node cart.test.mjs` para ver el fallo.
2. Encontrá el bug y corregilo **solo en `src/cart.mjs`**. No modifiques `cart.test.mjs`.
3. Terminá cuando `node cart.test.mjs` imprima `ok` y salga con código 0.
